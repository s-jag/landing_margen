# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project dashboard: Settings > API

# Public URL - safe to expose in browser
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon key - safe to expose, uses RLS for security
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key - NEVER expose in browser, server-side only
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# RAG API CONFIGURATION
# =============================================================================
# State-specific RAG APIs are configured below. The app uses state-based routing
# to automatically direct queries to the appropriate API based on client state.

# Legacy fallback (used if state-specific URL not set)
RAG_API_BASE_URL=https://florida-tax-rag-production.up.railway.app
RAG_API_KEY=your-rag-api-key

# =============================================================================
# STATE-SPECIFIC RAG APIs (Production)
# =============================================================================

# Florida Tax RAG API (supports streaming, source drill-down)
FLORIDA_RAG_API_URL=https://florida-tax-rag-production.up.railway.app

# Utah Tax RAG API v2 (supports tax forms, authority levels, tax type classification)
UTAH_RAG_API_URL=https://utah-tax-api-v2-production.up.railway.app

# =============================================================================
# APP CONFIGURATION
# =============================================================================

NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# EMAIL (Resend)
# =============================================================================
# Get your API key from https://resend.com

RESEND_API_KEY=re_xxxxx
CONTACT_EMAIL=your-email@example.com

# =============================================================================
# AI DOCUMENT EXTRACTION (Anthropic Claude)
# =============================================================================
# Get your API key from https://console.anthropic.com
# Used for extracting financial data from uploaded PDFs

ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# =============================================================================
# OPTIONAL: RATE LIMITING (Upstash Redis)
# =============================================================================
# Only needed if you want rate limiting in production

# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token
