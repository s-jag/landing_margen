import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up | Margen',
  description: 'Create your Margen account',
};

export default function SignupPage() {
  return <SignupForm />;
}
